server {
    listen 80;
    listen [::]:80;
    server_name localhost www.localhost;
    root /var/www/html;
    index index.html;

    location / {
		autoindex		on;
		try_files		$uri $uri/ =404;
        try_files $uri $uri/ /index.php$is_args$args;
	}
}
